<% if page.current? %>
  <span class="bg-blue text-white px-3 py-1 rounded-md border border-blue"><%= page.number %></span>
<% else %>
  <%= link_to page.number, orders_path(page: page), class: "px-3 py-1 hover:bg-blue hover:bg-opacity-50 border border-blue rounded-md" %>
<% end %>
